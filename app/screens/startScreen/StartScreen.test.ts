import StartScreen from './StartScreen';

describe('testing Start Screen', () => {
   test('sanity test', () => {
      expect(1).toEqual(1);
      expect(1).not.toEqual(2);
   });

   test('Start Screen renders', () => {
      expect(1).toEqual(1);
   });
});
